# Core (CPU wheels resolve by default; see README for the CUDA index URL)
torch>=2.4
numpy>=1.26
sentencepiece>=0.2.0
PyYAML>=6.0
tqdm>=4.66

# Dataset streaming (only needed by scripts/preprocess.py)
datasets>=3.0

# Tests
pytest>=8.0
